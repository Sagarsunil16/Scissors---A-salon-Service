# Frontend Application Layer

This folder is the domain-driven application layer for the frontend.

Each feature owns its API adapters, local model/types, feature-specific components,
and hooks. Shared visual primitives stay in `src/shared`, while route-level screens
compose feature modules from `src/pages`.
